{
  "name": "bamfield-peer-api",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute bamfieldsurfreport --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute bamfieldsurfreport --local --file=./schema.sql",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
